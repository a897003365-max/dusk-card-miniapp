# 晚霞来信 · 2026-09-06

**源码检查通过；微信开发者工具 CLI 运行验收未完成。**

## 交付范围

独立项目 `/Users/maizi/AI-Jobs/Projects/dusk-card-miniapp`，原 `love-diary-miniapp` 未被本任务修改。复制 22 家族 × 4 稀有度共 88 张公仔图，重做信匣、邮票卡面和旅行手账界面。没有复用原钱包、玩家存档、娃娃机玩法或正式 AppID。

三页分别为 `pages/home/index`、`pages/collection/index`、`pages/story/index`。功能含单封/五封抽取、两级保底、重复羁绊、三人编队、4 章 12 幕 24 选择、8 封章节回信、纪念品、历史回信重读与本地断点续读。

## 已验证

- `npm test`：37/37，通过抽取边界、10/40 封保底、五封逐次结算、重复计数、余额拒绝、合法编队、完整四章、两个结局、重访奖励、防重复结算、存储失败不扣除与坏档保留。输出 `unit.log`。
- `node tests/claw-wxss.test.js --native`：4 份真实 WXSS 原生编译通过，已知非法 `*` 写法被编译器实际拦截。输出 `wxss.log`。
- 微信工具自带 `wcc`：3 份实际 WXML 原生编译通过。输出 `wxml.log`。这不是页面运行验收。
- `node scripts/check-package-budget.js --json`：主包约 0.56 MiB，通过项目 1.90 MiB 预算阈值。原始字节数见 `budget.log`。
- 88 个卡面路径全部存在；初始队伍在 16 组四章选择路线中均能完成故事与保存结局。
- 独立代码审查已修正坏档覆写风险、WXML 换行、满羁绊提示、换队后的固定角色旁白、跨分支线索与历史回信入口。

## CLI 阻塞

微信开发者工具登录有效，Codex 客户端无需额外令牌，Skill 版本匹配 0.3.9。执行：

```sh
/Applications/wechatwebdevtools.app/Contents/MacOS/wechatide -c Codex project_import --project /Users/maizi/AI-Jobs/Projects/dusk-card-miniapp
```

返回 `APPID_ERROR`，原始文案为“`不存在此 AppID 请检查后重新输入`”。当前 `touristappid` 被 CLI 明确拒绝。错误字段见 `cli-import-blocker.json`。

已请用户选择现有独立测试号或另一个 AppID，并授权在临时副本进行 6 次抽取及四章剧情验收；尚未收到答复。未绑定原项目正式 AppID，未消耗任何真实存档中的资源，未上传、发布。

## 待验收

三页的实际显示、节点几何、点击/切换/滚动/关闭、六次抽取揭晓、编队、四章剧情、存档续读、历史回信与控制台。暂无模拟器截图，也未进行 iPhone 真机测试。

获得所需 AppID 与授权后，运行准备脚本创建没有存档的新副本，使用正常初始六张邮票，禁止注入余额/卡牌或清真实存储：

```sh
node scripts/prepare-acceptance.js
node tests/runtime-smoke.js <上一步输出的临时项目目录> --allow-progress
```

该脚本仅允许本项目准备器创建的系统临时目录，截图/报告先写系统临时目录；完成后归档，不用静态结果替代运行结果。
