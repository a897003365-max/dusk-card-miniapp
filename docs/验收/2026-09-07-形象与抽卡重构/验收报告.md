# 形象与抽卡重构 · 当前阶段验收

**当前完成分级演出、收藏重温和共用旧邮局背景；未完成全套角色图替换。** 新角色方向仍待用户从实际样板中选择。108项测试或下面的动画验收都不能代替全目标完成证明。

## 实际完成的内容

- 三种原创角色方向经过两轮生成与目视修订，当前比较图是 `docs/visual-rework/pilots/directions-v2.png`。22位角色差异化设定已写入 `CHARACTER_BRIEFS.md`；这些是设定和方向稿，不是22位已完成运行画稿。
- R纸页、SR双层花信、SSR星环、UR日蚀双轨的原生演出已接入；自启动至稳定约820/1180/1560/2150ms。三段计时只控制显示，不调用抽取随机数或再次提交存档。
- 支持跳过、下一封、直接查看全部、关闭与切后台；拥有的形态可以免费重温，返回保留原伙伴和形态。未拥有形态没有重温入口，调用端与消费端均重新检查所有权。
- 原创1024×1536旧邮局背景已接入首页和演出，运行JPEG约303KiB；文字、按钮、角色与特效仍由原生页面独立绘制。中央留出角色区域，边缘以窗、信柜、灯和木桌建立环境。

## 原生证据

使用独立测试号和系统临时隔离副本 `dusk-card-acceptance-2DBUUV`。从正常初始6票开始，四章实际获得12票；未注入余额、卡牌、保底或随机数。当前这份隔离存档累计18次抽取，余0票。未对真实存档执行消耗型验收。

| 目录 | 结果及范围 |
| --- | --- |
| `01-首轮单抽` | 首次真实单抽与一次入库验证；图中发现按钮默认外边距/宽度问题，后续修正 |
| `02-五封溢出发现` | 五封入库正确，逐张第二列信封被真实几何检查拦住；失败原样保留 |
| `03-四档实抽与录像` | 正常游戏累计第16抽取得UR，四档均实际演出并录制；存档、计数、按钮与控制台检查通过。此目录UR为发现遮挡问题的初版，不能称作最终UR |
| `04-跳过成功但录制受锁屏阻塞` | 实际点击跳过后读取到settled、animation-name none、opacity 1、transform none；随后OS锁屏使录像没有完整帧，保留fail，不伪装录像成功 |
| `05-跳过后关闭` | 继续检查第17次实际抽取已经保留，关闭不改资源 |
| `06-切后台` | 第18次实际抽取后切走再返回，当前卡已揭且不重播，永久状态不变；本轮没有请求OS录像 |
| `07-零票重温` | 0票下逐档通过重温、返回原伙伴/形态、未拥有入口隐藏和零存档改变验证 |
| `08-UR日蚀修订` | 修复后从真实已拥有UR重温入口复录，独立逐帧确认信封先让位、完整暗日与金边出现，再接角色；没有新遮挡或突兀跳位 |
| `10-原创背景复验` | 新背景接入后，390宽度四档角色、名称、按钮均完整可见，操作≥44px，控制台无应用异常；不外推为320/430通过 |

视频使用专用ScreenCaptureKit独立窗口过滤器：核验窗口ID、完整标题、进程、应用路径与可执行文件，静音、只使用现有权限，窗口身份变化即失败，没有全屏或区域回退。源片为2500×1746的开发者工具窗口，手机裁切版另存review目录；裁切没有修改游戏画面。播放器30fps是输出目标，不代表真机游戏性能结论。窗口静止时仅复用最后的真实窗口帧保留时长。

系统screencapture窗口ID/屏幕区域探测不适合此任务，相关探测文件已丢弃，未进入本归档。保留的是精确绑定窗口的合法录制与CLI截图。

## 独立复核后的修正

1. 关闭按钮受原生auto margin影响，继续按钮默认为184px；限定外边距及最小/最大宽度，复验通过。
2. 五封信的内部按钮超出第二列；限定为所在卡片100%宽度，后续真实逐张流程通过。
3. 跳过后新创建的角色节点仍播放入场；仅arrive阶段启动动画，settled立即显示，真实CLI样式检查通过。
4. UR暗日被信封遮住；信封先收缩让位，完整暗日停留后再展开。修订录像约1.9秒开始让位，2.0–2.2秒完整暗日可见，独立复核已确认。

## 代码检查

- `npm test`：108/108。
- 10份实际WXSS原生编译通过，非法通配选择器探针被拦截。
- 8份实际WXML原生编译通过。
- 主包约1.02MiB，三个冒险分包均低于1.9MiB阈值。该预算尚未计入待制作的新角色，不能外推为最终美术包体。
- 131份运行文件与已测副本一致，App隔离存档KEY归一后比较。详见 `09-源码检查/`。

## 尚未完成的目标

- 用户选定角色方向后，继续完整制作与替换22位伙伴、相关形态/姿态、敌人及场景细节；当前运行角色仍沿用旧图。
- 新资产透明边缘、手机阅读尺寸、一致性、分包预算及全部目标页面复验。
- 新版320/430宽度验收。Mac曾恢复可操作并完成UR复录，但在准备切换设备时再次锁屏；已请求用户手动解锁，不降低或绕过锁屏设置。
- 新背景与全套新角色接入后的整体运动复核、真实iPhone验收。未上传、发布、提交或推送。

完整目标保持活跃，当前不是“完美完成”结论。后续以真实文件、运行状态和用户对样板的明确选择继续推进。

交付目录已实际打开至信匣首页，新的旧邮局背景正常显示。只读前后比对一致：v2、390px、6邮票、0抽取、0星线，无存档错误；见 `11-交付首页/`。
