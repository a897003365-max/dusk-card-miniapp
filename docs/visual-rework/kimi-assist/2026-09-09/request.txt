Write one concise standalone Swift command-line script for macOS 14+ Apple Silicon using Apple Vision VNGenerateForegroundInstanceMaskRequest to extract all foreground instances from an input PNG and write a same-size truly transparent PNG, keeping original character RGB colors and antialiasing. The image is an original full-body illustrated paper sheep courier on an opaque gray checkerboard. Inputs are absolute input/output paths, refuse to overwrite or modify input, refuse empty mask. Use VNInstanceMaskObservation.generateMaskedImage(ofInstances:from:croppedToInstances:false) if API supports that, or scaled mask and CoreImage blend. Print only a JSON success summary. Provide Swift code then a short caveat about fine cords/holes needing visual inspection. Do not propose Python or external downloads, do not execute tools. Only one image, no batch and no model training. Code should compile via xcrun swiftc. Keep response under 150 lines.